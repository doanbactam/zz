# Task: add-feature-with-tests

Add a `capitalize` function to `src/lib.rs` that capitalizes the first letter of a string.

Requirements:
- Function signature: `pub fn capitalize(s: &str) -> String`
- Empty string returns empty string
- Single character returns it uppercased
- First character uppercased, rest unchanged
- Add at least 3 unit tests for the new function

Do not modify existing functions or tests.
