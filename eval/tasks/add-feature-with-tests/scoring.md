# Scoring: add-feature-with-tests

1. **1 point** if `capitalize` function exists with correct signature
2. **1 point** if empty string is handled (returns "")
3. **1 point** if at least 1 test for `capitalize` exists and passes

Maximum score: 3/3.
