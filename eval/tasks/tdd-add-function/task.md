# Task: tdd-add-function

Using test-driven development:
1. First, write a test in `src/lib.rs` called `test_multiply` that
   verifies `multiply(3, 4) == 12`.
2. Then, implement the `multiply(a: i32, b: i32) -> i32` function in
   `src/lib.rs` so the test passes.
